<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.whensaturdaycome.persuratan">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppThemes" />
        <activity android:name=".SuratMasukActivity" />
        <activity android:name=".SuratKeluarActivity" />
        <activity android:name=".DisposisiActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".InformasiActivity" />
        <activity android:name=".PilihSuratMActivity" />
        <activity android:name=".PilihDisposisiActivity" />
        <activity android:name=".MainStaffActivity" />
        <activity android:name=".PilihSuratKActivity" />
        <activity android:name=".PilihInformasiActivity" />
        <activity android:name=".TampilGambar" />
        <activity android:name=".Update.DiterimaSMActivity" />
        <activity android:name=".SplashScreenActivity"></activity>
    </application>

</manifest>